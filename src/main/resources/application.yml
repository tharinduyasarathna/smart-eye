spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartdb?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port : 8085
